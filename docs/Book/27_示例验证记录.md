# 第 27 章　示例验证记录

[上一章](26_从教材走进EasyFrp源码.md) · [总目录](00_阅读地图与学习方法.md)

本记录说明这套教材在编写时实际做过哪些验证，也说明这些检查不能证明什么。后续你修改示例后，应重新运行与改动有关的验证，而不是把本记录当成新版本的保证。

## 验证环境

- 编写日期：2026 年 9 月 20 日。
- 操作系统：Windows，命令示例采用 PowerShell。
- Python：3.12.10。
- PyQt6：6.11.0。
- 构建时 Qt 版本 `QT_VERSION_STR`：6.11.0。
- 实际运行时 Qt 版本 `qVersion()`：6.11.1。
- 自动界面检查采用 `QT_QPA_PLATFORM=offscreen`，不依赖人工点击屏幕。

验证使用本机已有解释器和依赖，没有为了编写教材额外安装库。第 03 章提供的虚拟环境命令供读者独立配置学习环境；本次未实际创建该虚拟环境。

## 完整示例验证

| 示例 | 实际检查内容 | 结果 |
|---|---|---|
| 第一课：你好窗口 | 按钮多次点击、标签计数、Qt 父对象关系 | 通过 |
| 第二课：注册表单 | 空白、未确认、短密码、正常提交、密码不回显、重置字段 | 通过 |
| 第三课：信号练习 | 滑块与数字框同步、相同值不重复通知、加一/归零、边界夹取、信号屏蔽恢复 | 通过 |
| 第四课：多页面窗口 | 切换页面保留输入、对话框空值校验和合法提交 | 通过 |
| 第五课：模型表格 | 范围校验、模型修改/插入/删除通知、数值排序、代理排序后删除源记录 | 通过 |
| 第六课：设置与文件 | 中文 UTF-8 保存、失败与取消保留修改、关闭取消、重开先保存、坏编码保护、临时 INI 设置 | 通过 |
| 第七课：后台任务 | 完成、防重复启动、取消、模拟失败、运行中关闭，共 5 类行为 | 通过 |
| 第八课：进程日志 | 完整输出、UTF-8 跨块和无换行末尾、启动失败、停止、运行中关闭，以及启动阶段立即停止 | 通过 |
| 第九课：网络请求 | 本机 HTTP 200、404、响应大小上限、取消、超时分支、运行中关闭，共 6 类行为 | 通过 |

网络检查使用临时本机 HTTP 服务提供可控制的响应；超时检查缩短了同一个计时器的间隔，以触发和验证超时分支，没有实际等待默认的完整 10 秒。它不等于公网 HTTPS、证书、代理及各种网络环境都已通过。

章节内另有一些完整程序或片段：第 09 章验证了定时和空格暂停；第 10 章验证了非模态窗口的复用、销毁清引用与重建；第 11 章验证了数字排序和记录编号；第 19 章运行了纯函数测试、QTest 点击和 QSignalSpy 计时器示例；第 21 章运行了临时 SQLite 示例。依赖现有变量或应用入口的片段按其上下文检查，不能直接把任意一个代码框当独立程序运行。

上述小示例行为检查在编写时通过临时验证脚本执行，没有全部保存为长期自动测试套件。你可以根据各章验收要求复现；综合项目的关键测试则已保存在教材中。

## 综合项目的可重复测试

| 测试文件 | 数量 | 覆盖范围 |
|---|---:|---|
| [测试任务仓库.py](综合实战/测试任务仓库.py) | 13 | 中文往返、空列表、无效任务、文件缺失、坏 JSON、过深嵌套、版本、字段、重复编号、替换失败保留旧文件 |
| [测试任务窗口.py](综合实战/测试任务窗口.py) | 10 | 添加、空输入、筛选后操作、取消选中、保存取消、往返打开、坏文件、取消关闭、失败保存拒绝关闭、同路径重新打开 |

这 23 项测试均通过。界面测试用 `QTest` 操作按钮，文件使用临时目录，对话框使用预设回答。没有把测试数据写进 EasyFrp 配置。

在项目根目录运行：

```powershell
python "docs/Book/综合实战/测试任务仓库.py"
$env:QT_QPA_PLATFORM = 'offscreen'
python "docs/Book/综合实战/测试任务窗口.py"
Remove-Item Env:QT_QPA_PLATFORM
```

此处设置与删除环境变量只影响当前 PowerShell 会话。若你此前已设置其他 Qt 平台值，测试后应恢复原值。使用虚拟环境时，把 `python` 替换为实际解释器路径。

## 文档与语法自检

[教材自检.py](示例代码/教材自检.py) 检查全部教学 Python 文件的语法和警告、Markdown 中行内本地链接的文件是否存在，以及 Python/Markdown 文件名是否含中文。它跳过 `.venv`，不运行示例、不联网、不检查外部网址，也不检查 Markdown 标题锚点。项目内本地源码链接也包含在路径检查中。

本次最终检查覆盖 **16 个 Python 文件、30 个 Markdown 文件、164 个本地文件链接**，发现问题数为 0。编号文档共 28 份（00～27），另外两份文档是示例与综合实战的运行说明。

```powershell
python "docs/Book/示例代码/教材自检.py"
```

文档中参考官网链接用于继续查阅对应主题；本次并未对每个外部网页做联网存活检查。Python 绑定调用形式在本机 PyQt6 环境中核对，其他版本的重载与可用模块仍需检查。

## 外观预览与人工验收范围

![综合实战界面预览](插图/综合实战预览.png)

这张预览由实际任务管理器离屏渲染生成，使用演示任务数据。它可以帮助你对照控件区域，但不能代表所有 Windows 主题、字体、输入法与显示缩放下的效果。

仍需你在实际目标环境检查的内容包括：中文输入法、键盘焦点、原生文件对话框、100%/150%/200% 缩放、多屏移动、打包资源、公网 HTTPS、外部数据库驱动和操作系统差异。本次没有执行 PyInstaller 构建，也没有完成干净机器安装验收；第 20 章给出的是操作方法和验收清单。

线程取消验证基于能够定期返回的小步骤，不能证明任意第三方阻塞函数都可取消。进程示例只管理自己的演示子进程，不包含跨平台子孙进程树清理。资源系统、Designer 和可选扩展模块取决于额外工具及平台，未声称全部安装或逐项运行。

验证的目的，是让“我觉得应该正确”变成“我用这些具体场景观察到这些结果”。把这个习惯带到你自己的项目中，比记住一份通过记录更有价值。
